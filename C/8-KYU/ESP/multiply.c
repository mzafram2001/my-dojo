// NOMBRE: Multiplicar
// AUTOR: jhoffner
// NIVEL: 8 Kyu
//
// DESCRIPCIÓN:
// Este código no se ejecuta correctamente. Intenta averiguar por qué.
//
// Esta es una función en C que multiplica dos enteros y devuelve el resultado.
int multiplicar(int a, int b) {
  // La función toma dos parámetros, 'a' y 'b', ambos de tipo int.
  // La declaración 'return' calcula el producto de 'a' y 'b' usando el operador '*'
  // y devuelve el resultado al que llama a la función.
  return a * b;
}